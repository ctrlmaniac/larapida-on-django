{
  "name": "larapida-on-django",
  "private": true,
  "workspaces": [
    "packages/*",
    "clients/*"
  ],
  "scripts": {
    "collectclients": "python collectclients.py",
    "build:clients": "npx lerna exec --concurrency 1 -- yarn build"
  },
  "dependencies": {
    "npm": "^8.12.1"
  },
  "devDependencies": {
    "lerna": "^5.1.1"
  }
}