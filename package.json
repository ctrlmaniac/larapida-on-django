{
    "name": "root",
    "private": true,
    "workspaces": [
        "clients/*"
    ],
    "scripts": {
        "build": "npx lerna exec --concurrency 1 -- yarn build",
        "collectclients": "bash collectclients.sh",
        "precollectclients": "yarn run build",
        "postcollectclients": "git add -A && git commit -m 'collected clients' && git push"
    },
    "devDependencies": {
        "lerna": "^5.5.0"
    }
}
